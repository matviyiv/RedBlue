# Generated by prepare-blue-zone.sh from blue-zone.config.sh — regenerated every
# run, never committed. Holds the per-folder blue-zone volume mounts layered on
# top of docker-compose.yml.
docker-compose.blue-zone.yml
.DS_Store