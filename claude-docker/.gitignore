# Generated by prepare-blue-zone.sh from blue-zone.config.sh — regenerated every
# run, never committed. Holds the per-folder blue-zone volume mounts layered on
# top of docker-compose.ai-sandbox.yml.
docker-compose.blue-zone.yml
.DS_Store

# Generated by ai-scripts/lib/blue-zone-browser.sh when the optional Playwright
# browser is enabled — regenerated every run, never committed. Holds the
# playwright-mcp / browser-proxy services and the generated egress allowlist.
docker-compose.browser.yml
